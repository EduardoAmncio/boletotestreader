import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  currentAccount: {
    marginTop: 16,
    backgroundColor: "transparent",
  },
  headerDivider: {
    marginBottom: 0,
  },
});
