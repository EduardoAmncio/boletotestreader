import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  currentAccount: {
    marginTop: 16,
    backgroundColor: "transparent",
  },
  seeAllButton: {
    marginBottom: 32,
  },
});
