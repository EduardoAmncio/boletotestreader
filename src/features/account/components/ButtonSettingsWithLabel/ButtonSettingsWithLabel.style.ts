import { makeStyles } from "@material-ui/core";

export const useStyle = makeStyles({
    buttonSettingsWithLabel: {
    width: "100%",
    backgroundColor: "#ffffff",
    color: "black",
    fontWeight: "initial",
    fontSize: "0.75rem",
    borderBottom: "1px solid #ccc",
    textTransform: "none",
    alignItems: "center",
    cursor: "pointer",
  },
});