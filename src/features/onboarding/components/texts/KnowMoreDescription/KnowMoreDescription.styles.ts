import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
    description: {
        fontWeight: 200,

    },
    link: {
        textDecoration: "none",
        color: "white",
        fontWeight: "bold",
    }
});