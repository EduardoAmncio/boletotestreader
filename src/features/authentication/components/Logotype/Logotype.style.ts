import { makeStyles } from "@material-ui/core";

export const useStyle = makeStyles({
  mainStyle: {
    display: "inline-block",
    height: "50px",
    background: "#323751",
    marginBottom: "32px"
  },

  logo: {
    
  }
});
