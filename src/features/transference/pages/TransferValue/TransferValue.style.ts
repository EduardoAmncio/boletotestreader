import { makeStyles } from "@material-ui/core";


export const useStyles = makeStyles({
    static: {
        display: "flex",
        flexDirection: "column",
        fontWeight: "bolder",
        marginTop: "-4%",
        marginLeft: "15px",
        fontSize: "1rem",
    },
    textInput: {
        marginTop: "10%",
    }
});